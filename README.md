#weather-app

This is a simple weather app that uses the weatherAPI to get the current weather for a given city. It also uses the Google Maps API to get the latitude and longitude of the current city.

instructions to run locally.

1. clone the repo
2. cd into the repo
3. run npm install
4. run npm start
5. open browser and go to localhost:3000
